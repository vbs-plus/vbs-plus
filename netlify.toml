[build.environment]
  NPM_FLAGS = "--version"
  NODE_VERSION = "16"

[build]
  publish = ".output/public"
  command = "npx pnpm@7.5.0 i --frozen-lockfile false && npx pnpm@7.5.0 build"
  ignore = "git diff --quiet HEAD^ HEAD . ../content"

[functions]
  node_bundler = "esbuild"
